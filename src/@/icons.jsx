import { SunMedium, Moon } from "lucide-react";
import React from "react";

// Sun icon
const SunIcon = (props) => <SunMedium {...props} />;

// Moon icon
const MoonIcon = (props) => <Moon {...props} />;

export const Icons = { SunIcon, MoonIcon };
